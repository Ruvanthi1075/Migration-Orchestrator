; client.conf — Stage 1: TLS client, default (classical) group negotiation
; Listens locally on 7000 (plaintext), wraps it in TLS, sends to 8443.
; See server.conf for why `groups` is omitted here for now.

client = yes
pid =

foreground = yes
debug = 6
output = /home/sahana/Migration-Orchestrator/migration/stunnel_configs/client.log

verify = 0

[classical-test]
accept = 127.0.0.1:7000
connect = 127.0.0.1:8443
